<%= form_for(@facebooklink) do |f| %>
  <% if @facebooklink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facebooklink.errors.count, "error") %> prohibited this facebooklink from being saved:</h2>

      <ul>
      <% @facebooklink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
